//a list of consecutive numbers from 50
#include <stdio.h>
int main()
{
	int i=50;//initialized the first number to be 50
	int count=1;
	while(count<=10)
	{
		printf("%d\n",i);
		i++;
		count++;
	}
	return 0;
}